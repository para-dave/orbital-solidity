[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200
via_ir = true

[profile.tempo]
src = "contracts"
out = "out"
libs = ["lib"]
# Tempo testnet RPC - update with actual URL from tempo.xyz
# rpc_url = "https://tempo-testnet-rpc.example.com"
# chain_id = <tempo_chain_id>

[rpc_endpoints]
tempo = "${TEMPO_RPC_URL}"

[etherscan]
tempo = { key = "${TEMPO_ETHERSCAN_KEY}" }
